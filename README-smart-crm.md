# Smart CRM status

The earlier SQLite/Supabase compatibility phase has been retired. Smart Persian intake, multi-phone matching, GPS/PostGIS proximity search, geocoding and route planning now run through the authenticated Flask + Neon production API in `app.py`.

For current setup and operational guidance, see `README.md`, `docs/OFFLINE_AND_ROUTING.md`, and `VERCEL.md`.
